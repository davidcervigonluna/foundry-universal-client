# OPTIONAL: these are only pre-filled DEFAULTS in the UI (fallback).
# The frontend is fully agnostic: if left empty, the user configures the Entra
# identity (tenant / client id) directly in the UI. These are NOT secrets.
VITE_ENTRA_CLIENT_ID=
VITE_ENTRA_TENANT_ID=
VITE_FOUNDRY_SCOPE=https://ai.azure.com/.default
